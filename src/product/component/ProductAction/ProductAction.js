/**
 * some actions for a product, such as `follow`, 'like', 'share' .etc
 * Usually you will use some kind of View lib, React/Vue for example
 */

export function renderProductAction(product) {
    return '';
}
